import { Router } from 'express';

const router = Router();

// External routes will be added here as features are implemented
// Example: Security routes (login, register, password recovery)

export default router;
